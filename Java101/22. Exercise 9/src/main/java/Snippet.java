		public class Snippet{
			public static void main(String args[]) {
				int numRows = 10;
				//Printing the 'numRows' rows
				for(int currentRow = 1 ; currentRow <= numRows ; currentRow++){			
					//Call the function getStarsString() with currentRow
					String stars = getStarsString(currentRow);			
					//to print new line
					System.out.println(stars);
				} 
			}
			
			public static String getStarsString(int currentRow) {
				//The number of stars in each row is same the the row number
				String stars = "";
				//For loop to add stars
				__________;
				return stars;
			}
		}
